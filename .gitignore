/target

# SEGA Saturn BIOS images live in bios/ and must never be committed —
# they are copyrighted by SEGA. Each developer supplies their own dump.
# Keep the directory documented via bios/README.md (whitelisted below).
bios/*
!bios/README.md

# CD-ROM disc images (CUE/BIN, CCD/IMG/SUB, ISO) live in roms/ and must
# never be committed — they are copyrighted. Each developer supplies their own.
# Keep the directory documented via roms/README.md (whitelisted below).
roms/*
!roms/README.md

# Presentation shaders (CRT filters for the planned SDL_GPU presenter) live in
# shaders/ — user-/third-party-supplied source + compiled SPIR-V/DXIL/MSL. Not
# committed (often GPL/CC-licensed presets, plus build artifacts); keep the
# directory documented via shaders/README.md (whitelisted below).
shaders/*
!shaders/README.md

# Reference emulators, used only as behavioral oracles for trace-diffing —
# local, never-committed source checkouts/builds. No emulator code is part of
# this project (it is referenced, not incorporated — see README Acknowledgements).
/mameref/
/yabref/
/mednaref/

# MAME reference runtime artifacts (the ./mameref/ oracle writes these to the
# repo root when run from here for boot-trace diffing). Never commit them.
/cfg/
/nvram/
/error.log
tmp/
